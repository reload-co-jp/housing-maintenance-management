type Team = { id: number; name: string }

type House = {
  id: number
  name: string
  latitude: number
  longitude: number
  createdAt: Date
}

type Position = { latitude: number; longitude: number }
